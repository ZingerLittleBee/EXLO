# PostgreSQL Configuration
POSTGRES_DB=exlo
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password

# Domain Configuration (Required for production)
DOMAIN=example.com
TUNNEL_URL=tunnel.example.com

# SSL/TLS Configuration (Let's Encrypt)
ACME_EMAIL=admin@example.com

# DNS Provider for wildcard certs (default: cloudflare)
# DNS_PROVIDER=cloudflare
CF_API_EMAIL=your-cloudflare-email@example.com
CF_DNS_API_TOKEN=your-cloudflare-api-token

# Web Dashboard Configuration
AUTH_SECRET=your-auth-secret-min-32-chars

# Internal API Configuration (shared between web and tunnel services)
# Must be at least 32 characters in production
INTERNAL_API_SECRET=your-internal-api-secret-min-32-chars
