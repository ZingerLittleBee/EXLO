---
title: 快速开始
description: 5 分钟内完成 EXLO 的部署和首次连接。
icon: Zap
---

import { Cards, Card } from 'fumadocs-ui/components/card'

## 环境要求

- Docker & Docker Compose
- 一个公网域名（如 `your.domain`）
- 支持通配符 DNS 或子域名配置

## 一键启动

```bash
# 克隆仓库
git clone https://github.com/ZingerLittleBee/EXLO
cd EXLO

# 复制环境变量模板
cp .env.docker.example .env

# 编辑配置
# 设置 POSTGRES_*、BETTER_AUTH_SECRET、INTERNAL_API_SECRET 和 API_BASE_URL
#（如果缺少 INTERNAL_API_SECRET 或 API_BASE_URL，请手动补充到 .env）

# 启动服务
docker compose up -d
```

## 首次初始化

如果是全新部署，请先创建管理员账户：

```
http://your.domain/setup
```

## 首次连接

### 1. 创建 SSH 隧道

```bash
ssh -N -R 80:localhost:3000 -p 2222 user@your.domain
```

参数说明：
- `-N`: 不执行远程命令
- `-R 80:localhost:3000`: 将远程 80 端口转发到本地 3000 端口
- `-p 2222`: SSH 服务端口

### 2. 完成 Device Flow 认证

连接后，终端会显示认证提示：

```
╭──────────────────────────────────────────────────╮
│  🔐 Device Activation Required                   │
│                                                  │
│  Visit: https://your.domain/activate             │
│  Code:  ABCD-1234                                │
╰──────────────────────────────────────────────────╯
```

在浏览器中打开链接并输入验证码完成认证（验证码 5 分钟过期）。

### 3. 访问你的服务

认证成功后，你的本地服务将通过分配的子域名访问：

```
https://your-subdomain.your.domain
```

## 断开连接

在隧道终端中双击 `ESC` 键即可断开连接。

也可以按下 `Ctrl+C` 结束 SSH 隧道。如果是在嵌套 SSH 或无法退出，请使用以下转义序列：

```
按下 Enter，然后输入 ~.
```

## 断线重连

EXLO 支持断线重连功能：

- **30 分钟内重连**：无需重新进行 Device Flow 认证，直接恢复隧道
- **保持相同子域名**：重连后使用与之前相同的子域名

> [!NOTE]
> 重连识别基于 SSH 公钥指纹。如果更换设备或 SSH 密钥，需要重新认证。

## 使用 autossh 自动重连

推荐使用 `autossh` 实现断线自动重连：

```bash
# 安装 autossh
# macOS
brew install autossh
# Ubuntu/Debian
apt install autossh

# 使用 autossh 连接
autossh -M 0 \
  -o "ServerAliveInterval=30" \
  -o "ServerAliveCountMax=3" \
  -o "ExitOnForwardFailure=yes" \
  -R 80:localhost:3000 -p 2222 user@your.domain
```

参数说明：
- `-M 0`：使用 SSH 内置的心跳机制（ServerAlive）
- `ServerAliveInterval=30`：每 30 秒发送心跳
- `ServerAliveCountMax=3`：连续 3 次无响应则重连
- `ExitOnForwardFailure=yes`：端口转发失败时退出（便于 autossh 重试）
