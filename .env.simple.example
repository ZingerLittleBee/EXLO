# Simple Deployment Environment Variables (No Domain Required)
# Copy this file to .env and modify as needed
# Usage: docker compose -f docker-compose.simple.yml up -d --build

# PostgreSQL Configuration
POSTGRES_DB=exlo
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_PORT=5432

# Host Configuration
# Use "localhost" for local access, or your server IP for remote access
HOST=localhost

# Port Configuration
WEB_PORT=3000
SSH_PORT=2222
PROXY_PORT=8080

# Web Dashboard Configuration
# Must be at least 32 characters
BETTER_AUTH_SECRET=simple-dev-secret-min-32-chars!!

# Environment Mode
NODE_ENV=development
RUST_ENV=development

# Tunnl Configuration
RUST_LOG=debug

# Access URLs after deployment:
# - Web Dashboard: http://localhost:3000 (or http://<HOST>:<WEB_PORT>)
# - SSH Connection: ssh -R 80:localhost:<your-local-port> <code>@localhost -p 2222
# - Tunnel Access: http://localhost:8080 (requires Host header or subdomain routing)
